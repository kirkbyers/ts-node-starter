{
  "name": "advent-of-code-2017",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "rimraf ./dist && tsc && concurrently 'tsc -w' 'nodemon --harmony ./dist/index.js'",
    "test": "rimraf ./dist && tsc && concurrently 'tsc -w' 'jest --watch ./dist'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/jest": "^21.1.8",
    "concurrently": "^3.5.1",
    "jest": "^21.2.1",
    "nodemon": "^1.12.1",
    "rimraf": "^2.6.2",
    "tslint": "^5.8.0",
    "typescript": "^2.6.2"
  }
}
